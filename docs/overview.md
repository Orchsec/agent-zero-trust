# Overview

`agent-zero-trust` evaluates AI agent configurations against deterministic Zero Trust rules.
It runs offline, makes no external API calls, and produces terminal, Markdown, or JSON reports.
